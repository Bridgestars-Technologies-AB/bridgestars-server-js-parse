import axios from 'axios'
// import DB from '../DB'
import { removeAllAssociatedSessions, isAdminAsync } from '../util';


//also leave the role.
//params: chatId
// Parse.Cloud.define("leaveChat", (req : Parse.Cloud.FunctionRequest) => { 
  
// })

// //maybe not necessary, user should have access to the chat object anyway (if he is in the chat) (via the role)
// //params: chatId, userId
// Parse.Cloud.define("addUserToChat", (req : Parse.Cloud.FunctionRequest) => { 
  
// })